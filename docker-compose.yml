services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: stalemiss
    restart: unless-stopped
    depends_on:
      mongo:
        condition: service_healthy
    volumes:
      - ./data:/usr/app/data
    environment:
      MONGO_HOST: mongo
      MONGO_PORT: 27017
      MONGO_DB: stalcraft_emissions
      PYTHONUNBUFFERED: 1
    command: ["python", "-u", "src/main.py"]
    logging:
      driver: local
      options:
        max-size: "10m"
        max-file: "3"

  mongo:
    image: mongo:7
    container_name: stalemiss-mongo
    restart: unless-stopped
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 60s
      timeout: 5s
      retries: 5
      start_period: 20s
    logging:
      driver: local
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  mongo_data:
